name: Collaboration progress report
# Scaffold template: source event triggers and cron schedules removed; run manually via workflow_dispatch.
on:
  workflow_dispatch:
permissions:
  contents: read
  pull-requests: read
concurrency:
  group: progress-report
  cancel-in-progress: false
jobs:
  report:
    if: github.repository == 'ramincsy/Gthub-Achievements'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out the trusted default branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Token-bearing workflow: pin the default branch even for workflow_dispatch.
          repository: ${{ github.repository }}
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          allow-unsafe-pr-checkout: false
      - name: Set up Node.js 22
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'
      - name: Count merged pulls and pair-path trailers on the default branch
        run: node scripts/progress.mjs
        env:
          GH_TOKEN: ${{ github.token }}
